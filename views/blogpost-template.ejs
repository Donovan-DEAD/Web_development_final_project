<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><%= blog.blog_blocks[0] ? blog.blog_blocks[0].blog_title : 'Blog Post' %></title>
        <link rel="icon" type="image/svg+xml" href="/images/favicon.svg">
        
        <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
        
        <!-- Custom Styles -->
        <link rel="stylesheet" href="/styles/navbar.css">
        <link rel="stylesheet" href="/styles/blogpost.css">
        
        <!-- Google Fonts -->
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wdth@0,75..100;1,75..100&family=Roboto:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
        
        <style>
            body {
                font-family: 'Arial', sans-serif;
                color: #333;
                background-color: #fff;
                line-height: 1.6;
            }
            
            .blog-header {
                background: #f9fafb;
                padding: 3rem 0;
                margin-top: 2rem;
                text-align: center;
            }
            
            .blog-header h1 {
                font-size: 2.5rem;
                margin-bottom: 1rem;
                color: #16a34a;
            }
            
            .blog-header h3 {
                color: #555;
                font-size: 1.25rem;
                font-weight: 400;
                margin-bottom: 1.5rem;
            }
            
            .blog-meta {
                display: flex;
                align-items: center;
                justify-content: center;
                gap: 1.5rem;
                color: #666;
                font-size: 0.95rem;
            }
            
            .blog-meta-item {
                display: flex;
                align-items: center;
                gap: 0.5rem;
            }
            
            .blog-meta-icon {
                width: 20px;
                height: 20px;
                opacity: 0.7;
            }
            
            .blog-content-wrapper {
                background: #fff;
                padding: 3rem 0;
            }
            
            .blog-content {
                max-width: 800px;
                margin: 0 auto;
            }
            
            .blog-content img {
                max-width: 100%;
                height: auto;
                border-radius: 8px;
                margin: 1.5rem 0;
            }
            
            .blog-content p {
                margin-bottom: 1.5rem;
                font-size: 1.1rem;
                line-height: 1.8;
            }
            
            .blog-content h2 {
                color: #16a34a;
                margin-top: 2rem;
                margin-bottom: 1rem;
            }
            
            .blog-content h3 {
                color: #333;
                margin-top: 1.5rem;
                margin-bottom: 0.75rem;
            }
            
            .no-content {
                text-align: center;
                padding: 3rem;
                color: #999;
                font-size: 1.2rem;
            }
        </style>
    </head>
    
    <body>
        <%- include('partials/navbar', { username: username, current_page: 'blogpost' }) %>

        <!-- Blog Header Section -->
        <section class="blog-header">
            <div class="container">
                <h1><%= blog.blog_blocks[0] ? blog.blog_blocks[0].blog_title : 'Blog Post' %></h1>
                <h3><%= blog.blog_blocks[0] ? blog.blog_blocks[0].blog_description : 'Blog Description' %></h3>
                <div class="blog-meta">
                    <div class="blog-meta-item">
                        <svg class="blog-meta-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                        </svg>
                        <span><%= blog.author %></span>
                    </div>
                    <div class="blog-meta-item">
                        <svg class="blog-meta-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                        </svg>
                        <span><%= new Date(blog.publication_date).toLocaleDateString('es-ES', { day: 'numeric', month: 'long', year: 'numeric' }) %></span>
                    </div>
                </div>
            </div>
        </section>

        <!-- Blog Content Section -->
        <section class="blog-content-wrapper">
            <div class="container">
                <div class="blog-content">
                    <% if (locals.blog && blog.blog_blocks) { %>
                        <% blog.blog_blocks.forEach(block => { %>
                            <%- include('partials/_' + block.type, { block: block }) %>
                        <% }); %>
                    <% } else { %>
                        <p class="no-content">Blog post content not available.</p>
                        <a href="/blog-search">Go back to search</a>
                    <% } %>
                </div>
            </div>
        </section>

        <!-- Bootstrap JS Bundle -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>